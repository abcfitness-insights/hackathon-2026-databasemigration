"""Deterministic rule pack.

Built-in rules live under ``rules.checks``; the registry in
``rules.registry`` enumerates the active set. Teams can supply additional
regex-based rules via ``rules.yaml_rules``.
"""
